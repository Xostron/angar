# 📑 Алгоритм чтения модулей

## 1. Модуль исправен

-   Начало цикла
-   Состояние модуля - ОК
-   Данные прочитаны

## 2. Модуль неисправен

-   Цикл 1
-   1. Состояние модуля - неисправен
-   2. Модуль помещен в список "Антидребезга" на 60сек `(см. п. Резюме)`
-   Цикл 2
-   3. Проверка модуля -> Модуль в списке антидребезга -> время антидребзга истекло ?
-   `НЕТ`: Разрешить повторный опрос -> если ошибка чтения сохранилась -> повтор чтения в течении времени антидребезга
-   `ДА`: Вывод сообщения: Модуль неисправен
-   Цикл 3
-   4. Время аварии: Игнор чтения неисправного модуля `(см. п. Резюме)`
-   Цикл 4
-   5. Время аварии истекло ->
-   `НЕТ`: ждем
-   `ДА`: Повторное чтение модуля
-   6. Данные успешно прочитаны?
-   `НЕТ`: повтор `пункт 5`
-   `ДА`: то модуль восстанавливается в работу (аварийное сообщение удаляется)

# 📙 Резюме:

-   Время антидребезга, время опроса, время аварии настраивается в админке для каждого модуля отдельно: клиент-склад-модуль
-   Время антидребезга - время в течении которого система пытается прочитать модуль
-   Время опроса - максимальное время в течении которого система ожидает ответа от модуля
-   Время аварии - Время в течении которого система не опрашивает неисправный модуль, по истечению данного времени система делает 1 попытку чтения модуля, если ответ получен, то модуль считается исправным и попадает в основной цикл чтения (сообщение о неисправном модуле удаляется), иначе модуль остается неисправным
-   Повтор

# 📌 FAQ:

-   Время опроса - при слишком малом значении, система может не успеть получить ответа от модуля, поэтому минимальное время опроса 0.3 с (с осторожностью)

-   Время антидребезга - Если данное время поставить слишком большим, то система все это время будет думать, что модуль исправен и не будет выводить ошибку о модуле
