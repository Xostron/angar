# 1.📑 Алгоритм чтения модулей

## 1.1. Модуль исправен

-   1. Начало цикла
-   2. Опрос модуля
-   3. Данные прочитаны
-   4. Повтор с п.1

## 1.2. Модуль неисправен

### Начало цикла

-   1. Проверка модуля, Опрос модуля Х
-   2. Ответ с ошибкой -> Модуль Х неисправен
-   3. Модуль Х помещен в список "Антидребезга" (по-умолчания на 60 сек) `(см. п. Резюме)`

### Начало цикла

-   1. Проверка модуля Х -> Модуль Х в списке антидребезга -> время антидребзга истекло?
-   2. `НЕТ`: Разрешить повторный опрос
-   3. Модуль Х неисправен

### Начало цикла

-   1. Проверка модуля Х -> Модуль Х в списке антидребезга -> время антидребзга истекло?
-   2. `ДА`: Модуль Х помещен в список неисправных (по-умолчания на 60 сек)
-   3. Вывод сообщения о несправности модуля Х

### Начало цикла

-   1. Проверка модуля Х -> Модуль Х в списке неисправных -> время аварии истекло?
-   2. `НЕТ`: Модуль остается в списке неисправных

### Начало цикла

-   1. Проверка модуля Х -> Модуль Х в списке неисправных -> время аварии истекло?
-   2. `Да`: Разрешение на один опрос модуля Х
-   3. Ответ с ошибкой -> Модуль Х неисправен
-   4. Модуль Х остается в списке неисправных (по-умолчания на 60 сек)

### Начало цикла ...ожидание времени аварии модуля

### Начало цикла

-   1. Проверка модуля Х -> Модуль Х в списке неисправных -> время аварии истекло?
-   2. `Да`: Разрешение на один опрос модуля Х
-   3. Ответ получен
-   4. Модуль Х удалется из списка неисправных и антидребезга, удаляется сообщение о неиспарвности

# 2. 📙 Настройки:

-   Время антидребезга, время опроса, время аварии настраивается в админпанели для каждого модуля индивидуально. Путь в админпанели: клиент - склад - модуль
-   Время антидребезга - время, в течении которого система пытается прочитать модуль
-   Время опроса - максимальное время, в течении которого система ожидает ответа от модуля
-   Время аварии - время, в течении которого система не опрашивает неисправный модуль, по истечению выполняется 1 попытка чтения модуля, если ответ получен, то модуль считается исправным и попадает в основной цикл чтения (сообщение о неисправном модуле удаляется), иначе модуль остается неисправным

# 3. 📌 Внимание:

-   Время опроса - при слишком малом значении, система может не успеть получить ответа от модуля, поэтому минимальное время опроса 0.3 с (с осторожностью)
-   Время антидребезга - Если данное время поставить слишком большим, то система все это время будет думать, что модуль исправен и не будет выводить ошибку о модуле
