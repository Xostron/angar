# ♻️ Предупреждение "Неправильные настройки гистерезиса"

Антидребезг плавного пуска ВНО по давлению/темп_канала: `./src/control/extra/extralrm/stableVno`

## 🔱 Условия работы

-   Склад ВКЛ
-   Секция в авторежиме
-   Тип складов: обычный, комбинированный

## 📑 Описание:

### Дребезг количества включенных ВНО

#### Алгоритм:

-   Формируем очередь acc.queue `el={count:кол-во включенных ВНО, date:время изменения}` - Размер очереди равен `_LIMIT=5`, здесь фиксируются кол-во включенных релейных ВНО, наполение очереди посделедовательное, когда очередь переполняется удаляется первый элемент очереди.
-   Из очереди, сравниваем крайнюю пары и проверяем что разница между первой и последней записью была не более `Настройки вентиляторов (Антидребезг плавного пуска=30сек)`

#### Пример 1

acc.queue = [1,2,1,2,1]: 1 такт Работает ВНО1, 2 такт ВНО1+ВНО2, 3 такт ВНО1, 4 такт ВНО1+ВНО2, 5 такт ВНО1.
Сравниваем пары из массива друг с другом (acc.queue?.[0]?.count === acc.queue?.[2]?.count && acc.queue?.[1]?.count === acc.queue?.[3]?.count && acc.queue?.[2]?.count === acc.queue?.[4]?.count),
🔶Установка предупреждения:
Если пары равны и время между начальной и конечной записью меньше времени "Антидребезг плавного пуска (системные настройки)" => Предупреждение "Неправильные настройки гистерезиса".
Также система фиксирует кол-во включенных ВНО (в большую сторону, поэтому в данном примере, будет 2 ВНО работать) и будет всегда включать 2 ВНО, пока не нажмут на кнопку "Сброс аварии"

#### Пример2

[1,2,3,4,5]: 1такт ВНО1, 2такт ВНО1+ВНО2, 3такт ВНО1+2+3 - дребезга нет
Сравниваем пары из массива друг с другом (queue[0]=1 != queue[2]=3 ), пары не равны - Все Ок




### (отменен с 10.11.2025, Андросов С.) 2.2 Дребезг частоты ПЧ (частое изменение частоты ПЧ)

#### Алгоритм:

-   Принцип такой же как и в п.2.1, только вместо фиксации кол-ва ВНО, фиксируется задание частоты в % [50,60,50,60,50]
    При возникновении дребезга по частоте, возникает предупреждение "Неправильные настройки гистерезиса" и фиксируется частота ПЧ на 100%, пока не нажмут на кнопку "Сброс аварии"
