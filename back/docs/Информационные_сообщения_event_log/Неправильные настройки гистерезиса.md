# Предупреждение "Неправильные настройки гистерезиса"

# 1. 📑 Описание:

Тип склада: для обычных и комбинированных
ВНО - напорный вентилятор.
Данное предупреждение распространяется только при работе обычных вентиляторов в авто режиме. (Прочитать про плавный пуск, можно здесь ./Плавный пуск.md). Плавный пуск контроллирует кол-во включенных ВНО в зависимости от давления/температуры в канале. При неправильных настройках гистерезиса (слишком заниженное значение) система может войти в колебательный процесс, для предотвращения колебаний введено предупреждение "Неправильные настройки гистерезиса", которое "замораживает" кол-во включенных ВНО (если алгоритм пытался включить 3 вентилятора, будет зафиксировано 3 ВНО, при наличии ВНОО с ПЧ, задание на ПЧ установится на 20%). Предупреждение сбрасывается оператором через мобильное приложение или POS-терминал, выкл склада или перевод секции в другие режимы.

# 2. 🔱 Условия работы

-   Тип складов: обычный, комбинированный
-   Склад ВКЛ
-   Секция в авторежиме

# 3. Путь к настройкам

Настройки -> Настройки вентиляторов

# 4. Описание настроек

1. Антидребезг плавного пуска - если за данное время были колебания во включении-отключении одного и того же ВНО, то установится предупреждение (заводское значение: 30 сек)
2. Гистерезис давления (заводское значение: 10 Па)
3. Задержка включения вентилятора
4. Давление в канале в зависимости от влажности

# 5. Описание (программист)

Путь: `./src/control/extra/extralrm/stableVno`
Код `stableVno`
Принадлежность: секция

1.  Формируем очередь acc.queue, где элемент очереди `el={count:количество ВНО, date:время запуска}` (count = -1..n), где -1 - ноль запущенных ВНО, 0 - запущен 1 ВНО и т.д.
2.  Размер очереди равен `_LIMIT=5`. Наполнение очереди происходит по сравнению в каждой итерации текущего и предущего кол-ва включенных ВНО.
3.  При переполнении очереди, удаляется первый элемент.
4.  Анализ очереди начинается когда она достигает `_LIMIT=5`.
5.  Для срабатывания предупреждения необходимо два условия: 1. Сравнение пар по полю count. 2. Сравнение первой и последней записей по полю date (разность между последней и первой записью больше времени `Настройки вентиляторов (Антидребезг плавного пуска=30сек)`)
6.  Сброс аварии по кнопке, выкл склада, первод секции из авто в другие режимы

# Пример 1

acc.queue = [0,1,0,1,0]: 1 такт Работает ВНО1, 2 такт ВНО1+ВНО2, 3 такт ВНО1, 4 такт ВНО1+ВНО2, 5 такт ВНО1.
Сравниваем пары из массива друг с другом (acc.queue?.[0]?.count === acc.queue?.[2]?.count && acc.queue?.[1]?.count === acc.queue?.[3]?.count && acc.queue?.[2]?.count === acc.queue?.[4]?.count) и разность времени между последней и первой записями меньше `Настройки вентиляторов (Антидребезг плавного пуска=30сек)` - Предупреждение установлено

# Пример2

[-1,0,1,2,3]
1 нет ВНО, 2 такт ВНО1, 3 такт ВНО1+ВНО2, 4 такт ВНО1+2+3 - дребезга нет, 5 такт ВНО1+2+3+4
Одинаковых пар нет - Все Ок
